apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: faiss-index
  labels:
    app: context-resolver
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 200Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: graph-embeddings
  labels:
    app: context-resolver
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Gi
